import * as React from "react";

import { ButtonTypes } from "../enums";

const toggleonClick = (isClicked: boolean, setIsClicked: any, type: any) => {
  setIsClicked(!isClicked);
};



export default toggleonClick;
