export enum ButtonTypes {
  PLAY = "play",
  MUTE = "mute",
  CINEMA_MODE = "cinema mode",
  FULLSCREEN = "full screen",
  SMALL_MODE = "small mode",
}
export enum Keys {
  SPACE = 32,
  LEFT = 37,
  RIGHT = 39,
  ESC = 27,
}
